export type LikeSentChain = {
  total: number;
};

export type LikeSentTargetChain = {
  target: Buffer[];
};

export type LikedChain = {
  status: 0 | 1;
};

export type LikeChain = {
  address: Buffer[];
};
