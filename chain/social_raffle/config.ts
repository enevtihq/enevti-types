export type SocialRaffleConfig = {
  socialRaffle: {
    maxPrice: string;
    rewardsCutPercentage: number;
    blockInterval: number;
    maxRaffledPerCollection: number;
    maxRaffledPerProfile: number;
  };
};
